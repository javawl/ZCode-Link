{
  "name": "@zcode/backlinks-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Native ZCode backlink publishing tools, persistent browser and publishing skills migrated from link-harness 2.0.0.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/mcp/server.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src --no-ignore",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "@zcode/backlinks": "workspace:*",
    "playwright-core": "1.59.1",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
